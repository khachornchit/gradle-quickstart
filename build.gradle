/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * User Manual available at https://docs.gradle.org/5.4.1/userguide/java_library_plugin.html
 */

plugins {
    // Apply the java-library plugin to add support for Java Library
    id 'java-library'
}

repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

dependencies {
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api 'org.apache.commons:commons-math3:3.6.1'

    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    implementation 'com.google.guava:guava:27.0.1-jre'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
}

class Greeting extends DefaultTask {  
    String message 
    String recipient

    @TaskAction 
    void sayGreeting() {
        println "${message}, ${recipient}!" 
    }
}

tasks.register("hello", Greeting) { 
    group = 'Welcome'
    description = 'Produces a world greeting'
    message = 'Hello' 
    recipient = 'World'
}

task task1 {

	group "group1"
	description "Description for task1()"
	doLast {
		println "Executed task1().";
	}
	
}

task task2 {

	group "group1"
	description "Description for task2()"
	doLast {
		println "Executed task2().";
	}
	
}

task copyFile(type: Copy) {

	group "group2"
	description "Description for copyFile()"
	from 'C:\\Core\\test1\\data.txt'
	into 'C:\\Core\\test2'
	doLast {
		println "Executed copyFile() task.";
	}	
}

task greeting {

	group "group3"
	description "Description for greeting()"
	doFirst {
		println "Good morning from greeting task doFirst() !"
	}
	
	doLast {
		println "Good morning from greeting task doLast() !"
	}
}
